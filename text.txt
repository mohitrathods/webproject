-----------------------------
tasks 
- return->this ?
> class arrays string integer http query > 25 - 25 methods
> loops arrays 

> array vado task 

- in all, fetch created_at  and updated_at and set updated_at and created_at
- validataion in all actions
- in salesman > salesman price vadu 
- in product > product media 
- category > parent category
- understand > request, front, index, action, adapter 


if($controllerName != 'prodctmedia'){
            $resultClassName = new $strreplace();
            print_r($resultClassName);
        }


 if($controllerName != 'productmedia'){
            $strreplace = str_replace("Controller_Product_Media","Controller/Product/Media",$controllerClassName).'.php';
        print_r($strreplace);
        }

if($controllerName != 'productmedia'){
            
        print_r($controllerClassName);
        
        }