<?php
$vendors = $this->getVendor();
?>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="skin/css/edit.css?<?php echo time(); ?>">
    <title>Edit data</title>
</head>
<body>
<form method="post" action="<?php echo $this->getUrlObj()->getUrl('vendor', 'update' , ['id' => $vendors['vendor_id']]) ?>">
    <div class="mainadd">
        <div class="eachdiv">
            <label>FIRST NAME</label>
            <input type="text"  name="vendor[first_name]" value="<?php echo $vendors['first_name'] ?>" />
        </div>

        <div class="eachdiv">
            <label>LAST NAME</label>
            <input type="text"  name="vendor[last_name]" value="<?php echo $vendors['last_name'] ?>" />
        </div>

        <div class="eachdiv">
            <label> EMAIL</label>
            <input type="email"  name="vendor[email]" value="<?php echo $vendors['email'] ?>" />
        </div>

        <div class="eachdiv">
            <label>GENDER</label>
            <select name="vendor[gender]">
                <option value="male <?php if($vendors['gender'] == 'male') {echo 'selected';} ?>">Male</option>
                <option value="female <?php if($vendors['gender'] == 'female'){echo 'selected';} ?>">Female</option>
            </select>
        </div>

        <div class="eachdiv">
            <label>MOBILE</label>
            <input type="tel"  name="vendor[mobile]" value="<?php echo $vendors['mobile'] ?>" />
        </div>

        <div class="eachdiv">
            <label>STATUS</label>
            <select name="vendor[status]">
                <option value="active <?php if($vendors['status'] == 'active'){echo 'selected';} ?>">Active</option>
                <option value="inactive <?php if($vendors['status'] == 'inactive'){echo 'selected';} ?>">Inactive</option>
            </select>
        </div>

        <div class="eachdiv">
            <label>COMPANY</label>
            <input type="text"  name="vendor[company]" value="<?php echo $vendors['company'] ?>" />
        </div>

        <div class="eachdiv">
            <input type="submit" class="submitbutton" />
            <a href="<?php echo $this->getUrlObj()->getUrl('vendor', 'grid',[], true) ?>">CANCEL</a>
        </div>
    </div>
</form>
</body>
</html>